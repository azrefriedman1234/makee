pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Needed for TDLib (tdlibx)
        maven { url = uri("https://jitpack.io") }
    }
}
rootProject.name = "PasiflonetMobile"
include(":app")
